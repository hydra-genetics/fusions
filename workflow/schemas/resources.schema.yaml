$schema: "http://json-schema.org/draft-04/schema#"
description: snakemake configuration file
type: object
properties:
  default_resources:
    type: object
    properties:
      threads:
        type: integer
        description: default number of threads that will be used by a rule
      time:
        type: string
        description: default max execution time for a rule
      mem_mb:
        type: integer
        description: default memory used for a rule
      mem_per_cpu:
        type: integer
        description: default memory used per cpu for a rule
      partition:
        type: string
        description: default partition to use on the cluser for a rule

  required:
    - default_resources

  gene_fuse:
    type: object
    properties:
      threads:
        type: integer
        description: number of threads that will be used by 'gene_fuse'
      time:
        type: string
        description: max execution time for 'gene_fuse'
      mem_mb:
        type: integer
        description: memory used for 'gene_fuse'
      mem_per_cpu:
        type: integer
        description: memory used per cpu for 'gene_fuse'
      partition:
        type: string
        description: partition to use on the cluser for 'gene_fuse'
